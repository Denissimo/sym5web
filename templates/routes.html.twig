     {% block header %}
            {% include('header.html.twig') %}
     {% endblock %}

        <div class="page-content"> 

            <div class="page-header">
                <div class="container">
                    <div class="row">
                        <h2 class="page-title">
                            Мои БВС
                        </h2>
                    </div>
                </div>
            </div>
            
        </div>

    {% block footer %}
            {% include('footer.html.twig') %}
    {% endblock %}